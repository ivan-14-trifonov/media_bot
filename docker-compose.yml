version: "3.8"

services:
  transcribe:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - uploads_data:/app/uploads
      - results_data:/app/results

volumes:
  uploads_data:
  results_data:
